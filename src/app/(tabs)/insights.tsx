import InsightsScreen from "@/screens/insights";

/**
 * Insights tab route — the merged History + Reports screen (segmented
 * History/Reports toggle). Thin re-export; real content lives in
 * `src/screens/insights`.
 */
export default InsightsScreen;
